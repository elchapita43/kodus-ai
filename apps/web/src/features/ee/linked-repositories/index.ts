/**
 * Linked repositories settings UI — Enterprise Edition.
 * Path under `features/ee/` → Codus Enterprise License (`license_ee.md`).
 */
export { LinkedRepositories, MAX_LINKED_REPOSITORIES_UI } from "./linked-repositories";
export {
    addLinkedRepository,
    normalizeLinkedRepositories,
} from "./linked-repositories-state";
