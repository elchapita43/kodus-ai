---
title: "Referencia de Comandos"
description: "Referencia completa de todos los comandos, flags y opciones del CLI de Codus."
---

## Flags Globales

Estos flags están disponibles en todos los comandos:

| Flag              | Corto | Predeterminado | Descripción                                   |
| ----------------- | ----- | -------------- | --------------------------------------------- |
| `--format <fmt>`  | `-f`  | `terminal`     | Formato de salida: `terminal`, `json`, `markdown` |
| `--output <file>` | `-o`  | —              | Guardar la salida en un archivo               |
| `--verbose`       | `-v`  | `false`        | Salida detallada                              |
| `--quiet`         | `-q`  | `false`        | Modo silencioso (solo errores)                |
| `--agent`         | —     | `false`        | Modo agente: salida determinista legible por máquina |

---

## `codus review`

Analiza los archivos modificados en busca de problemas de calidad de código, errores, vulnerabilidades de seguridad e infracciones de estilo.

### Uso

```bash
codus review                          # Revisar cambios del árbol de trabajo (interactivo)
codus review [files...]               # Revisar archivos específicos
codus review --staged                 # Revisar solo archivos preparados
codus review --branch main            # Comparar contra una rama
codus review --commit abc123          # Analizar un commit específico
codus review --fix                    # Aplicar automáticamente todos los problemas corregibles
codus review --prompt-only            # Salida optimizada para agentes de IA
```

### Flags

| Flag                   | Corto | Descripción                                                 |
| ---------------------- | ----- | ----------------------------------------------------------- |
| `--staged`             | `-s`  | Analizar solo archivos preparados                           |
| `--commit <sha>`       | `-c`  | Analizar el diff de un commit específico                    |
| `--branch <name>`      | `-b`  | Comparar la rama actual contra la rama especificada         |
| `--rules-only`         | —     | Revisar usando solo las reglas configuradas (sin sugerencias generales) |
| `--fast`               | —     | Modo rápido: análisis más ágil con verificaciones más ligeras |
| `--focus <text>`       | —     | Dirige la revisión a un área específica — consulta [Dirigir una Revisión](/how_to_use/es/code_review/review_directive) |
| `--heavy`              | —     | Pasada más profunda: rondas extra de crítico para mayor recall (alpha, ver abajo) |
| `--interactive`        | `-i`  | Modo interactivo: navegar y aplicar correcciones            |
| `--fix`                | —     | Aplicar automáticamente todos los problemas corregibles     |
| `--prompt-only`        | —     | Salida optimizada para agentes de IA                        |
| `--fail-on <severity>` | —     | Salir con código 1 si los problemas alcanzan o superan la severidad |
| `--context <file>`     | —     | Archivo de contexto personalizado para incluir en la revisión |
| `--fields <csv>`       | —     | Seleccionar campos de respuesta (modo JSON/agente), p. ej. `summary,issues.file` |
| `--github-pat <token>` | —     | Token de GitHub (`read:repo`) usado para clonar repositorios privados. Solo usuarios en trial. También vía `CODUS_GITHUB_PAT`. Se mantiene solo en memoria, nunca se persiste. |
| `--no-hunk`            | —     | Omite el visor hunk y usa la lista interactiva antigua (solo sesiones interactivas) |

<Warning>
  `--heavy` es una función **alpha**, **con acceso bajo petición** — está desactivada para todas
  las organizaciones hasta que la habilitemos. Solo cloud por ahora; no disponible en self-hosted.
  Hasta que te habilitemos, la flag se acepta pero la revisión se ejecuta con normalidad, sin
  error ni aviso. Solicita acceso en [Discord](https://discord.gg/TFZBRk9fT6) o en
  [support@kodus.io](mailto:support@kodus.io). Consulta
  [Heavy mode](/how_to_use/es/code_review/review_directive#heavy-mode).
</Warning>

### Modos de Revisión

<Tabs>
  <Tab title="Interactivo (Predeterminado)">
    Navega por archivos con recuentos de problemas, previsualiza las correcciones sugeridas y aplícalas una a una.

    ```bash
    codus review
    ```

  </Tab>
  <Tab title="Corrección Automática">
    Aplica todos los problemas corregibles a la vez con una confirmación.

    ```bash
    codus review --fix
    ```

  </Tab>
  <Tab title="Agente de IA">
    Salida mínima y estructurada diseñada para Claude Code, Cursor o Windsurf. Permite bucles autónomos de revisión y corrección.

    ```bash
    codus review --prompt-only
    ```

  </Tab>
</Tabs>

---

## `codus pr suggestions`

Obtiene sugerencias con IA para pull requests abiertos.

### Uso

```bash
codus pr suggestions --pr-url https://github.com/org/repo/pull/42
codus pr suggestions --pr-number 42 --repo-id <id>
codus pr suggestions --pr-number 42 --repo-id <id> --severity error,warning
codus pr suggestions --pr-number 42 --repo-id <id> --format json
```

### Flags

| Flag                   | Descripción                                |
| ---------------------- | ------------------------------------------ |
| `--pr-url <url>`       | URL del pull request                       |
| `--pr-number <number>` | Número del pull request (requiere `--repo-id`) |
| `--repo-id <id>`       | ID del repositorio para el pull request    |
| `--severity <list>`    | Severidades separadas por comas para incluir |
| `--category <list>`    | Categorías separadas por comas para incluir |
| `--fields <csv>`       | Seleccionar campos de respuesta (modo JSON/agente), p. ej. `summary,issues.file` |

---

## `codus pr business-validation`

Valida un diff local contra las reglas de negocio/criterios de aceptación de una tarea. Útil antes de fusionar para confirmar que la implementación coincide con la intención de la tarea.

### Uso

```bash
codus pr business-validation --task-url https://linear.app/team/issue/KC-1441
codus pr business-validation --task-id KC-1441 --staged
codus pr business-validation --task-id KC-1441 --branch main
codus pr business-validation --task-id KC-1441 --commit abc123
codus pr business-validation --task-id KC-1441 --dry-run
```

### Flags

| Flag                 | Corto | Descripción                                                        |
| -------------------- | ----- | ------------------------------------------------------------------ |
| `--task-url <url>`   | —     | URL de la tarea para agregar al comando de validación              |
| `--task-id <id>`     | —     | ID de tarea o clave de issue (p. ej. `KC-1441`) para agregar      |
| `--staged`           | `-s`  | Usar solo los cambios preparados                                   |
| `--commit <sha>`     | `-c`  | Usar el diff de un commit específico                               |
| `--branch <name>`    | `-b`  | Comparar la rama actual contra una rama base                       |
| `--dry-run`          | —     | Mostrar el payload sin ejecutar la llamada a la API                |

---

## `codus auth`

Gestiona la autenticación con la API de Codus.

### `auth login`

```bash
codus auth login                              # Inicio de sesión interactivo
codus auth login -e user@mail.com -p password # No interactivo
```

| Flag                    | Corto | Descripción            |
| ----------------------- | ----- | ---------------------- |
| `--email <email>`       | `-e`  | Correo de la cuenta    |
| `--password <password>` | `-p`  | Contraseña de la cuenta |

### `auth logout`

```bash
codus auth logout
```

Elimina la autenticación local (inicio de sesión y clave de equipo).

### `auth status`

```bash
codus auth status
```

Muestra el modo de autenticación, el correo, la validez del token, las organizaciones, el estado de prueba y los límites de uso.

### `auth team-key`

```bash
codus auth team-key --key codus_xxxxx
```

Autenticar usando una clave de API de equipo compartida. También se puede establecer mediante la variable de entorno `CODUS_TEAM_KEY`.

### `auth team-status`

```bash
codus auth team-status
```

Muestra los detalles de autenticación del equipo (nombre de la organización y del equipo).

### `auth token`

```bash
codus auth token
```

Genera un token de CI/CD para usar en pipelines automatizados. Úsalo con la variable de entorno `CODUS_TOKEN`.

---

## `codus hook`

Gestiona los hooks de Git pre-push para la revisión automática de código antes de enviar cambios.

### `hook install`

```bash
codus hook install                    # Predeterminado: fail-on critical, modo rápido habilitado
codus hook install --fail-on error    # Bloquear envíos con severidad error
codus hook install --no-fast          # Deshabilitar modo rápido
codus hook install --force            # Sobrescribir hook existente
```

| Flag                   | Predeterminado | Descripción                               |
| ---------------------- | -------------- | ----------------------------------------- |
| `--fail-on <severity>` | `critical`     | Severidad mínima para bloquear el envío   |
| `--fast` / `--no-fast` | `true`         | Habilitar/deshabilitar modo rápido        |
| `--force`              | —              | Sobrescribir hook existente sin preguntar |

### `hook uninstall`

```bash
codus hook uninstall
```

Elimina el hook pre-push instalado por Codus.

### `hook status`

```bash
codus hook status
```

Muestra el estado del hook, la severidad de fail-on, la configuración del modo rápido y la ruta.

### Omitir el Hook

```bash
CODUS_SKIP_HOOK=1 git push
```

---

## `codus config centralized`

Gestiona la configuración centralizada desde el CLI.

### Uso

```bash
codus config centralized status
codus config centralized init [owner/repo] --sync-option <pr|manual>
codus config centralized sync
codus config centralized disable
codus config centralized download --out ./centralized-config.zip
```

### Subcomandos

| Subcomando              | Descripción                                                                   |
| ----------------------- | ----------------------------------------------------------------------------- |
| `status`                | Mostrar si la configuración centralizada está habilitada y el repositorio fuente seleccionado |
| `init [repository]`     | Habilitar la configuración centralizada para un repositorio seleccionado      |
| `sync`                  | Ejecutar la sincronización de configuración centralizada bajo demanda         |
| `disable`               | Deshabilitar la configuración centralizada y limpiar el repositorio seleccionado |
| `download --out <path>` | Descargar el bundle ZIP de configuración centralizada                         |

### Flags

| Flag                           | Aplica a                    | Descripción                       |
| ------------------------------ | --------------------------- | --------------------------------- |
| `--sync-option <pr \| manual>` | `init`                      | Estrategia de sincronización. El predeterminado es `pr` |
| `--out <path>`                 | `download`                  | Ruta de salida requerida para el archivo ZIP |
| `--json`                       | todos los subcomandos centralizados | Salida JSON estructurada   |

Para una guía completa, consulta: [/how_to_use/en/code_review/configs/centralized_config#cli-dedicated-section](/how_to_use/es/code_review/configs/centralized_config#cli)

---

## `codus config repo`

Inspecciona y actualiza la configuración del repositorio almacenada en Codus. Se requiere autenticación con clave de equipo. Usa `.` para apuntar al repositorio actual.

<Note>`codus config repo` y `codus config remote` son alias del mismo grupo de comandos.</Note>

### Uso

```bash
codus config repo add                                   # Agregar el repo actual a Codus
codus config repo add owner/repo                        # Agregar un repo específico
codus config repo list                                  # Listar repos configurados
codus config repo show                                  # Mostrar la configuración del repo actual
codus config repo setup                                 # Asistente de configuración guiado
codus config repo set . reviewEnabled true              # Establecer una configuración directamente
codus config repo open                                  # Abrir el panel de Codus
codus config repo add-ignore-file . "**/*.generated.ts"
codus config repo add-base-branch . "release/*"
codus config repo add-ignore-title . "^WIP:"
```

### Subcomandos

| Subcomando                                         | Descripción                                                                 |
| -------------------------------------------------- | --------------------------------------------------------------------------- |
| `add [repository]`                                 | Agregar un repositorio a Codus (atajo: `codus config -r [repository]`)      |
| `list`                                             | Listar repositorios configurados en Codus                                   |
| `show [repository]`                                | Mostrar la configuración del repositorio                                    |
| `setup [repository]`                               | Ejecutar un asistente de configuración guiado del repositorio               |
| `set [repository] <key> <value>`                   | Establecer una configuración del repositorio directamente                   |
| `open [repository]`                                | Abrir el panel de Codus para configuraciones avanzadas                      |
| `add-pattern [repository] <field> <pattern>`       | Agregar un patrón a un campo de lista (`ignore-files`, `base-branches`, `ignore-titles`) |
| `remove-pattern [repository] <field> <pattern>`    | Eliminar un patrón de un campo de lista                                     |
| `add-ignore-file [repository] <pattern>`           | Atajo: agregar un patrón a los patrones de archivos ignorados               |
| `remove-ignore-file [repository] <pattern>`        | Atajo: eliminar un patrón de archivos ignorados                             |
| `add-base-branch [repository] <pattern>`           | Atajo: agregar un patrón de rama base                                       |
| `remove-base-branch [repository] <pattern>`        | Atajo: eliminar un patrón de rama base                                      |
| `add-ignore-title [repository] <pattern>`          | Atajo: agregar un patrón de título ignorado                                 |
| `remove-ignore-title [repository] <pattern>`       | Atajo: eliminar un patrón de título ignorado                                |

### Flags

| Flag          | Aplica a | Descripción                         |
| ------------- | -------- | ----------------------------------- |
| `--no-prompt` | `add`    | Omitir el aviso de configuración posterior a la adición |

---

## `codus rules`

Crea, actualiza y visualiza Cody Rules — reglas estructuradas que Codus aplica durante la revisión.

### Uso

```bash
codus rules view                                         # Ver todas las reglas de tu equipo
codus rules view --repo-id <id>                          # Filtrar por repositorio
codus rules view --uuid <rule-uuid>                      # Obtener una regla individual
codus rules create --title "No console.log" --rule "Avoid leaving console.log in committed code" --severity high
codus rules update --uuid <rule-uuid> --severity critical
```

### Subcomandos

| Subcomando | Descripción            |
| ---------- | ---------------------- |
| `create`   | Crear una nueva Cody Rule |
| `update`   | Actualizar una regla existente |
| `view`     | Ver Cody Rules         |

### Flags de `rules create`

| Flag                    | Predeterminado | Descripción                                     |
| ----------------------- | -------------- | ----------------------------------------------- |
| `--title <title>`       | —              | Título de la regla                              |
| `--rule <rule>`         | —              | Contenido/descripción de la regla               |
| `--repo-id <id>`        | `global`       | ID del repositorio (o `global` para aplicar a todos los repos) |
| `--severity <severity>` | `medium`       | `low`, `medium`, `high`, `critical`             |
| `--scope <scope>`       | `file`         | `pull request` o `file`                         |
| `--path <glob>`         | `**/*`         | Patrón glob para la selección de archivos       |
| `--json`                | `false`        | Mostrar la regla creada como JSON               |

### Flags de `rules update`

| Flag                    | Descripción                                     |
| ----------------------- | ----------------------------------------------- |
| `--uuid <uuid>`         | UUID de la regla a actualizar (requerido)       |
| `--repo-id <id>`        | ID de repositorio actualizado                   |
| `--title <title>`       | Título actualizado                              |
| `--rule <rule>`         | Contenido/descripción actualizado               |
| `--severity <severity>` | Severidad actualizada                           |
| `--scope <scope>`       | Ámbito actualizado                              |
| `--path <glob>`         | Patrón glob actualizado                         |
| `--json`                | Mostrar la regla actualizada como JSON          |

### Flags de `rules view`

| Flag             | Descripción                    |
| ---------------- | ------------------------------ |
| `--uuid <uuid>`  | Obtener una regla individual por UUID |
| `--repo-id <id>` | Filtrar reglas por ID de repositorio |
| `--json`         | Mostrar las reglas como JSON   |

---

## `codus skills`

Inspecciona e instala los skills de agentes incluidos con el CLI (Claude Code, Cursor, Codex).

### Uso

```bash
codus skills list                                        # Listar los skills incluidos
codus skills install                                     # Instalar en los directorios de agentes detectados
codus skills install --dry-run                           # Vista previa de los cambios
codus skills resync                                      # Resincronizar los skills incluidos
codus skills uninstall                                   # Eliminar los skills incluidos
```

### Subcomandos

| Subcomando  | Descripción                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `list`      | Listar los skills incluidos disponibles en este paquete del CLI             |
| `sync`      | Sincronizar los skills incluidos en los directorios de agentes locales detectados |
| `resync`    | Resincronizar los skills incluidos en los directorios de agentes locales detectados |
| `install`   | Instalar los skills incluidos en los directorios raíz de agentes detectados (crea dirs según sea necesario) |
| `uninstall` | Desinstalar los skills gestionados incluidos de los directorios de agentes detectados |

### Flags

| Flag        | Aplica a                    | Descripción                                    |
| ----------- | --------------------------- | ---------------------------------------------- |
| `--dry-run` | `install`, `sync`, `resync`, `uninstall` | Mostrar los cambios planificados sin escribir archivos |

<Tip>Para el bootstrap completo de múltiples agentes, prefiere el instalador de plataforma (`curl -fsSL https://review-skill.com/install | bash`). `codus skills` cubre la resincronización local y la limpieza.</Tip>

---

## `codus status`

Muestra un estado consolidado de Codus: modo de autenticación, organización, repositorio, hook, skills incluidos y versión del CLI.

```bash
codus status
```

Útil como primer diagnóstico cuando algo falla o al incorporar una nueva máquina.

---

## `codus diff`

Abre el changeset actual en el visor de diff de Hunk en la terminal.

```bash
codus diff
```

Es un wrapper fino sobre `hunk diff` — cada argumento que pases se reenvía sin cambios, así que
cualquier flag que Hunk soporte también funciona aquí.

<Note>
  Útil para revisar visualmente un changeset antes de ejecutar `codus review` sobre él. El mismo
  visor respalda la experiencia de revisión interactiva; pasa `--no-hunk` a `codus review` si
  prefieres la lista antigua.
</Note>

---

## `codus update`

Actualiza el CLI de Codus a la última versión desde npm.

```bash
codus update
```

Resuelve el comando de instalación global correcto para tu gestor de paquetes (`npm`, `yarn`, `pnpm`, `bun`) y lo ejecuta. Si se usó un script instalador, el CLI te indica el comando para volver a ejecutarlo.

---

## `codus subscribe`

Abre la página de precios/actualización de Codus en tu navegador.

```bash
codus subscribe
```

---

## Niveles de Severidad

Los problemas se categorizan por severidad, de mayor a menor:

| Nivel      | Descripción                                                      |
| ---------- | ---------------------------------------------------------------- |
| `critical` | Problemas graves de seguridad o estabilidad que requieren atención inmediata |
| `error`    | Errores o problemas significativos                               |
| `warning`  | Problemas potenciales que vale la pena abordar                   |
| `info`     | Sugerencias y mejores prácticas                                  |

## Categorías de Problemas

| Categoría                | Descripción                            |
| ------------------------ | -------------------------------------- |
| `security_vulnerability` | Problemas relacionados con la seguridad |
| `bug`                    | Posibles errores                       |
| `performance`            | Oportunidades de optimización del rendimiento |
| `code_quality`           | Mejoras en la calidad del código       |
| `best_practices`         | Infracciones de mejores prácticas      |
| `complexity`             | Problemas de complejidad del código    |
| `maintainability`        | Preocupaciones de mantenibilidad       |
| `style`                  | Problemas de estilo de código          |
| `documentation`          | Carencias de documentación (aceptado por `--category` en `codus pr suggestions`) |
