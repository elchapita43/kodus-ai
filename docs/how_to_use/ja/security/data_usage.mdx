---
title: "AI モデルのセキュリティ"
description: "どの AI モデルがコードを処理するのか、プロバイダーとの契約は誰が持つのか、レビュー中にコードに何が起きるのか。"
---

## どのモデルがコードをレビューするのか

固定のモデルはありません。**Codus はすべてのプランでデフォルトが BYOK（Bring Your Own Key）**
です。つまり、モデルと、その背後にあるプロバイダーとの関係は、通常は当社ではなくお客様のものです。

<Info>
  このページでは意図的にモデル名を固定していません。カタログの入れ替わりは速いため、現時点の
  推奨モデルは [BYOK](/how_to_use/ja/byok) を参照してください。
</Info>

経路は 3 つあり、どれに当てはまるかでデータに関する契約の相手が変わります：

| 経路 | プロバイダーアカウントの所有者 | モデルのクラス |
| --- | --- | --- |
| **BYOK**（デフォルト、全プラン） | **お客様。** お客様のキー、お客様の請求、お客様とプロバイダーの契約。 | 設定したもの — 汎用のフロンティアモデル、コード特化モデル、セルフホスト／互換エンドポイントなど。 |
| **トライアル／キー未接続** | Codus（回数制限あり）。 | 当社が選定したコード特化モデル。フォールバックとして小規模なオープンウェイトモデル。 |
| **Enterprise（Codus 管理のキー）** | Codus（お客様の Enterprise 契約に基づく）。 | 当社が選定した汎用のフロンティアモデル。 |

### サポートするプロバイダーのクラス

BYOK では、Codus を次のいずれにも向けられます：

- **ファーストパーティのフロンティアプロバイダー** — 主要な商用ラボの自社 API。
- **クラウドホスト型のエンタープライズゲートウェイ** — 同じフロンティアモデルを既存のクラウド
  アカウントで課金し、そのクラウドの IAM・リージョン固定・データレジデンシー保証を利用。
- **アグリゲーター** — 多数のモデルの前段に単一の請求関係。
- **OpenAI 互換／Anthropic 互換の任意のエンドポイント** — コード特化プロバイダーや、自社で
  ホストするモデル（この場合コードはネットワーク外に出ません）を含みます。

現時点の完全な一覧と設定手順は [BYOK](/how_to_use/ja/byok) にあります。

<Note>
  **BYOK のレビューが、黙って Codus 管理のモデルにフォールバックすることはありません。**
  設定したモデルが失敗し、かつ自前のフォールバックを設定していない場合、レビューは失敗します —
  お客様のコード（と請求）を当社インフラに迂回させることはしません。
</Note>

## Codus がコードに対して行うこと

- コードはそのレビューを生成するためだけに、必要な部分のみ（差分と、レビューに必要なコンテキスト）
  がモデルプロバイダーへ送られます。
- Codus が学習のために保持するものはありません。
- レビュー結果（サジェスト、メタデータ）は、履歴の表示・サジェストの重複回避・メトリクス算出の
  ために保存されます。

## プロバイダーがコードに対して行うこと

ここは注意して読む価値があります。**BYOK では、これは Codus との契約ではなく、お客様と
プロバイダーとの契約に支配されるからです。**

- 商用 API のほとんどのティアは、既定では API トラフィックを学習に使わないと明示しています。
  実際に契約しているプランで確認してください。無料／コンシューマー向けティアは有料 API ティアと
  条件が異なることがよくあります。
- 保持期間（不正利用監視のためのものなど）はプロバイダーが定めるもので、Codus ではありません。
- 契約上の保証（ゼロ保持、特定リージョン、署名済み DPA）が必要な場合は、プロバイダーから直接
  取得してください。あるいはクラウドゲートウェイやセルフホストのエンドポイントを使えば、
  それらの条件は既存の契約に含まれています。

<Warning>
  プロバイダーを選ぶのはお客様であるため、**Codus がプロバイダーに代わってデータ取り扱いを
  約束することはできません。** コンプライアン要件が特定の保証に依存する場合は、Codus を向ける
  前にプロバイダーに確認してください。
</Warning>

## Codus 側のセキュリティ対策

- **キーの取り扱い**：プロバイダーのキーは保存時に暗号化され、平文で表示し直されることはありません。
- **暗号化**：転送時・保存時ともにデータを暗号化。
- **アクセス制御**：ワークスペースへのロールベースアクセス —
  [ワークスペースのロール](/how_to_use/ja/workspace_roles) を参照。
- **セルフホスト**：コードを一切ネットワーク外に出したくない場合は、スタック全体を自社インフラ
  で運用できます。[Codus のデプロイ](/how_to_deploy/ja/deploy_codus/generic_vm) を参照。

## セルフホストのテレメトリ

Codus をセルフホストで運用している場合、各インスタンスは 1 日 1 回、集計されたカウンターと
ランタイムのメタデータだけを含む匿名のハートビートを `telemetry.kodus.io` に送信します —
コード、識別情報、ユーザーを特定しうるものは一切含まれません。送信内容は
`pnpm run telemetry:preview` で確認でき、`CODUS_TELEMETRY_DISABLED=true` で無効化できます。
スキーマ全体、保持ポリシー、ソースコードへのリンクは
[匿名テレメトリ](/how_to_deploy/ja/deploy_codus/telemetry) に記載しています。

ご質問はサポートチーム、または [Discord](https://discord.gg/TFZBRk9fT6) までどうぞ。
